# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(vortexTaskLib)
ADD_LIBRARY(${VORTEX_TASKLIB_NAME} SHARED 
    ${VORTEX_TASKLIB_SOURCES})
add_dependencies(${VORTEX_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${VORTEX_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${VORTEX_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VORTEX_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VORTEX_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${VORTEX_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${VORTEX_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VORTEX_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VORTEX_TASKLIB_HEADERS}
    DESTINATION include/orocos/vortex)

